# This properties file is filed and used only when the editor-app test are run with Maven. The db.user variable of
# of your test-user profile difined in the pom.xml will be the value of the name property, and the db.password
# the value of the password property.
# test-user should have read and write persmission on your test dabase.

name=${db.user}
password=${db.password}
