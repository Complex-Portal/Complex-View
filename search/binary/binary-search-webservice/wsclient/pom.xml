<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <parent>
        <groupId>uk.ac.ebi.intact.binarysearch.ws</groupId>
        <artifactId>binary-search-webservice</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>

    <artifactId>wsclient</artifactId>
    <packaging>jar</packaging>
    <version>1.0-SNAPSHOT</version>

    <name>IntAct Binary Search :: Web Service :: WS Client</name>
    <description>A web service interface to retrieve data from intact</description>

    <scm>
        <connection>scm:svn:https://intact.svn.sourceforge.net/svnroot/intact/repo/binary-search/search/trunk/binary-search-webservice/wsclient</connection>
        <developerConnection>scm:svn:https://intact.svn.sourceforge.net/svnroot/intact/repo/binary-search/trunk/binary-search-webservice/wsclient</developerConnection>
        <url>http://svn.sourceforge.net/viewcvs.cgi/intact/repo/binary-search/trunk/binary-search-webservice-ws/wsclient</url>
    </scm>

    <properties>
        <maven.test.failure.ignore>true</maven.test.failure.ignore>
        <jetty.port>24521</jetty.port>
        <ws.context>ws</ws.context>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>jaxws-maven-plugin</artifactId>
                <version>1.0-beta-1-EBI-20070310</version>
                <configuration>
                    <wsdlUrls>
                        <wsdlUrl>http://localhost:${jetty.port}/${ws.context}/binarysearch?wsdl</wsdlUrl>
                    </wsdlUrls>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>wsimport</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>xfire</groupId>
                        <artifactId>xfire-jsr181-api</artifactId>
                        <version>1.0-M1</version>
                    </dependency>
                </dependencies>
            </plugin>

            <plugin>
                <groupId>org.codehaus.cargo</groupId>
                <artifactId>cargo-maven2-plugin</artifactId>

                <configuration>
                    <wait>false</wait>

                    <container>
                        <containerId>jetty5x</containerId>
                        <type>embedded</type>
                    </container>
                    <configuration>
                        <properties>
                            <cargo.servlet.port>${jetty.port}</cargo.servlet.port>
                        </properties>
                        <deployables>
                            <deployable>
                                <groupId>uk.ac.ebi.intact.binarysearch.ws</groupId>
                                <artifactId>ws</artifactId>
                                <type>war</type>
                                <properties>
                                    <context>${ws.context}</context>
                                </properties>
                            </deployable>
                        </deployables>
                    </configuration>
                </configuration>
                <executions>
                    <execution>
                        <id>start-container</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>start</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>stop-container</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>stop</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>

    </build>

    <dependencies>

        <!-- Dependencies for cargo -->
        <dependency>
            <groupId>uk.ac.ebi.intact.binarysearch.ws</groupId>
            <artifactId>ws</artifactId>
            <version>${version}</version>
            <type>war</type>
            <scope>provided</scope>
        </dependency>

    </dependencies>

    <pluginRepositories>
        <pluginRepository>
            <id>codehaus snapshot repository</id>
            <url>http://snapshots.repository.codehaus.org/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
        </pluginRepository>
    </pluginRepositories>


    <distributionManagement>
        <site>
            <id>intact-site</id>
            <name>Intact site</name>
            <url>${site.remote.basedir}/binary-search/binary-search-webservice/${artifactId}</url>
        </site>
    </distributionManagement>

</project>
